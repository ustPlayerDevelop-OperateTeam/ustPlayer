# ===================== .NET 构建产物 =====================
[Bb]in/
[Oo]bj/
[Dd]ebug/
[Rr]elease/
[Ll]og/
[Ll]ogs/
artifacts/
publish/
*.user
*.suo
*.userosscache
*.sln.docstates
*.userprefs

# ===================== IDE / 编辑器 =====================
.vs/
.idea/
.vscode/*
!.vscode/tasks.json
!.vscode/settings.json
!.vscode/launch.json
!.vscode/extensions.json
*.swp

# ===================== 测试与覆盖率 =====================
[Tt]est[Rr]esult*/
coverage*.json
coverage*.xml
coverage*.info
*.coverage
*.coverage.*
BenchmarkDotNet.Artifacts/

# ===================== 打包 / 发布产物 =====================
*.nupkg
*.snupkg
# 各平台发布脚本输出
/release/
/dist/

# ===================== 原生运行资源（实体不入库，仅保留占位说明） =====================
# 视频渲染器（uPlRender 构建产物）：renderer/ustplayer_renderer.{dll,dylib,so}
# 由 CI 或本地脚本放入，运行时从 renderer/ 子目录加载
**/renderer/*
!**/renderer/README.md
!**/renderer/.gitkeep
# 内置 FFmpeg：ffmpeg/ffmpeg(.exe)、ffmpeg/ffprobe(.exe)
**/ffmpeg/*
!**/ffmpeg/README.md
!**/ffmpeg/.gitkeep
# ⚠️ 上面的规则按**目录名**忽略，而源码目录可能恰好同名：
#    ustPlayer.Tests/renderer/ 放的是渲染器集成测试**源码**（二进制由
#    build/sync-native-assets.ps1 也同步到同一目录）。曾经因此让 4 个测试文件
#    长期未入库——`git status` 里完全看不到，CI 也少跑一整套原生集成测试。
#    因此这里显式把源码扩展名重新纳入版本控制。新增源码目录名与构建产物目录同名时，
#    请同样在这里加一条否定规则。
!**/renderer/*.cs
!**/renderer/*.axaml
!**/ffmpeg/*.cs
!**/ffmpeg/*.axaml

# ===================== 用户本地数据（沿用 1.1.x 约定，不提交） =====================
Settings.ini
Settings.json
cache/
test/
"ustPlayer uplr sample/"

# ===================== 本地示例 / 转换器产物 =====================
uplr_converter.exe
*.obj
*.pdb
*.ilk

# ===================== 日志 =====================
*.log
ustPlayer.log
