## Unreleased

#### 🐛 问题修复

- 修复 .uplr 解压的路径穿越防护：拒绝带盘符 / 绝对路径的压缩条目，防止恶意工程文件把内容解压到缓存目录之外。
- 修复 .uplr 解压遇到目录条目直接报错的问题；解压改为分块流式写入并加入单成员 / 总量上限，防 zip bomb 耗尽内存或磁盘。
- 旧版文本 .uplr 经 C++ 转换器转换时，GBK / Shift-JIS 编码的内容会正确转成 UTF-8（中文项目名 / 路径 / 自定义文字不再乱码）。
- 播放器启动失败改用 ERcode005 提示（此前统一归入 ERcode999）。
- 切换 UST 编码后内容预览立即刷新（此前需改动路径或手动点「编码检查」才会重读）。
- `Settings.json` 改为临时文件 + 原子替换写入，避免崩溃/断电留下半截配置。
- 修复语言切换后「关于软件」区链接的提示文字不随语言更新。

#### 🏗️ 底层重构（面向开发者的变更）

- 类型检查：修复 pyright Standard 模式下的残留类型错误（当前 0 error）。
- 转换器输出的 `Info.json` 不再包含新版已移除的 `show_phoneme` / `show_midinote` / `show_waveform` 字段。

#### 🚀 构建与发布

- 发版机制调整：转换器（`uplr_converter`）改为**独立压缩包**发布，不再并入 ustPlayer 主包；ustPlayer 主包只打包编译产物文件夹内的内容，不再包含外层目录。
- 发版说明提取：ChangeLog 小节标题里的空格与 tag 名里的连字符现在可互相匹配（手动推送 `v*` 标签触发发版时不再丢失更新说明）。

# v1.1.0 Beta 1

## 🎉 新功能

- **全新 .uplr 工程格式（ZIP 容器）**：工程文件从一个"纯文本记事本"升级为一个"压缩包"，里面装着 `Info.json`（记录你的全部设置）+ 工程文件 / 歌词 / 伴奏音频。分享给朋友时一个文件全带走，不再需要手动配齐三个文件；**旧版的文本 .uplr 仍然可以正常打开**，不用担心旧工程作废。
- **伴奏音乐播放**：播放时可以为歌曲配上伴奏音频，画面与时间轴会跟着音乐同步走；没有配音频、或音频加载失败时，播放器会自动退回"纯画面 + 计时"模式，不影响使用。
- **窗口背景效果**（其他页 → 主题卡片）：新增 **关闭 / 亚克力模糊 / Mica** 三档窗口背景效果，跟随设置保存、重启后依然生效。Mica 仅 Windows 11 有效（其他系统自动回退纯色），亚克力在 Win10 / Win11 都可用。
- **拖拽导入**：把 `.uplr` 工程文件直接拖进主窗口就能导入（基础页）；**文件页**拖入 `.ust` 自动填入文件路径，**歌词页**拖入 `.lrc` 自动填入歌词路径——少点几次对话框。
- **C++ 转换器 `tools/uplr_converter`**：把旧版文本 .uplr 一键转成新版 ZIP 格式（鼠标交互界面 + 命令行两种用法，零第三方依赖），随每个 Release 一起发布。
- **多语言界面**：支持 **简体中文 / 文言（华夏）／English** 三种界面语言（其他页 → 语言卡片切换，即时生效、重启记忆）。默认**跟随系统语言**，也可以手动指定。切换语言后整个窗口（含侧边导航、卡片、按钮、提示、输入框占位符）即时重译，不需要重启。其中「文言（华夏）」以**繁体字文言文**呈现界面（如「播放 Play」作「奏之」）。
- **播放器字体自适应**：播放器全屏画面按界面语言选择字体（中文「等线」、英文 Segoe UI），英文界面下文字显示更清晰。

## ✨ 体验优化

- **页面卡片化**：各页面的"/ XXX"纯文字分区升级为卡片式分区（卡片头部带主题色竖线 + 分区名），基础 / 播放器 / 歌词 / 其他四个页面风格统一，界面更清爽。
- **「编码检查」真正有效**：以前只是让你"自己看预览内容猜编码对不对"，现在会真实地用当前编码去读文件，读不动就明确提示你换编码。
- **选择 LRC 歌词文件**时，起始目录与文件页保持一致，少走弯路。

## 🐛 问题修复

- 「歌词色」选项现在**真正生效**了：之前配置了但播放器 LRC 歌词渲染根本没用到这个颜色。
- 播放时间超过 1 小时不再显示错乱的分钟数（现在按 HH:MM:SS:CC 正确显示）。
- 伴奏音频播完时，优先取媒体总时长作为时间锚点，时间轴不再往回跳。
- 新版 .uplr **重复导入**前会先清空旧缓存目录，不会再把上次导入的过期资源混进来。
- 旧版文本 .uplr 支持 GBK / Shift-JIS 等多种编码读取；跨机器导致资源路径失效时，会记录日志提示而不是静默失败。
- 导出 .uplr 时，不同目录下的同名资源会自动重命名，避免 ZIP 内条目互相覆盖。
- 导出 .uplr 时未手打扩展名会自动补全 `.uplr`，导出的文件保证能再次导入。
- 设置文件（目录）只读时，读写自动回退到用户数据目录；读取时校验修正的默认值会写回文件。
- 配置读写失败改用日志记录，打包版不再因缺少控制台而崩溃。
- 日志文件位置调整：优先写入程序根目录，不可写时回退用户数据目录。
- 修复了 Pylance / pyright Standard 模式下的类型检查问题（接口返回类型、PySide6 枚举改用限定名等），开发体验更顺。
- ERcode008 / 009 已补登记进 ERcode.txt。

#### 🧹 移除

本版本没有移除任何功能

#### 🏗️ 底层重构（面向开发者的变更）

- **模块全面解耦**：所有功能统一通过 `AppContext` 服务门面与数据契约（`core/contracts.py`）互相调用，页面不再直接依赖核心实现；新增 `context.py` 作为唯一组装点，UI 页面只认 `ctx.settings / ctx.parser / ctx.player / ctx.project_io` 四个接口。
- **设置系统重构**：`SettingsManager` 拆分为六个设置子域（`core/settings/` 下的 project / file / display / color / player / theme，各自持有属性 + 信号 + 读写 + 校验），`SettingsManager` 只做组装与编排；UI 访问统一改为 `ctx.settings.<子域>.<属性>`。
- **设置持久化迁移至 `Settings.json`**：结构为「分组 → 键值」字典，键与旧 ini 段保持一致；旧版 `Settings.ini` 在首次启动时自动迁移，迁移成功后删除旧文件。
- **入口迁移**：程序入口迁至 `ustplayer.app`，`main.py` 仅作启动薄壳；`uv run main.py` 与 `uv run ustplayer` 两条入口等价。

#### 🚀 构建与发布

- **构建大幅提速**：依赖安装改用 uv（带缓存），Nuitka 并行编译 + 增量缓存，小改动时只重编受影响模块。
- **移除约 120MB 死依赖**：`pyside6-fluent-widgets` 不再带 `[full]` extra（去掉 scipy / numpy / pillow / colorthief），安装体积与构建时间都更小。
- **CI 修正**：所有分支的提交都会触发构建（`pass` 开头的提交跳过）；修复发版误触发——只有 `main` 分支上以 `v` 开头的提交或 `v*` 标签推送才会发版；同一分支的并发构建自动取消旧的。
- 发布工作流改为从 **ChangeLog.md** 提取版本说明（原 UPDATELOG.md 已更名）。

# v1.0.0

>[!CAUTION]
>
>ustPlayer 1.0.0 (v26f19)（当前新版本）仅支持 64位Windows10及更晚的版本 ，若您的设备不符合条件，请您使用uPl-v26b10的旧版本 —— 它同样被提交在该Release下。

新版本的ustPlayerform产出画面与旧版本无异，您可以选择对您的既有uPl进行升级迭代，也可以选择维持原有版本持续使用。

旧版本的.uplr工程文件可以被此Release版本读取，但此版本产生的工程文件将会包含旧版本不支持的选项。

## 更新内容

* 对GUI框架进行重写，由 Tkinter 全面迁移至 PySide6 + Fluent Design；
>- 用图标的侧边导航替代了原来的手绘标签页。
>- 采用 PySide6-Fluent-Widgets 组件库。
>- 原来基础页的复选框全部换成 Switch 按钮。
>- 操作成功/失败的提示改为顶部滑入的 InfoBar。
* 新增「音高线颜色」选项；
* 新增 loguru 日志模块 ，您可以在其产生的日志及控制台反馈文件中找到问题所在；
* 颜色选择从系统对话框升级为 Fluent 风格的内置取色面板；
* .ust文件的编码检查逻辑由原本"用户目视文件内容并确认其可读取性"变更为"触发式uPl自动检查功能"；
* 大范围调整了代码的结构，优化其他贡献者的体验；
* 优化了产出画面的清晰度；
* 增强了部分代码的鲁棒性。
