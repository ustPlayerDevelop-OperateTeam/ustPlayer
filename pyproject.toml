[project]
name = "ustplayer"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "loguru>=0.7.3",
    "pyside6>=6.11.1",
    "pyside6-fluent-widgets[full]>=1.11.3",
]

[project.scripts]
ustplayer = "ustplayer:main"

[build-system]
requires = ["uv_build>=0.12.2,<0.13.0"]
build-backend = "uv_build"
